apiVersion: v1
kind: Pod
metadata:
  name: foundation-pod
  labels:
    app: foundation-demo
spec:
  containers:
    - name: nginx
      image: nginx:1.28-alpine
      ports:
        - containerPort: 80
