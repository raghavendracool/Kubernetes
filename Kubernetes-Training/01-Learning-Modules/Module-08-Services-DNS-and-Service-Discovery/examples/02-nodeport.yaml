apiVersion: v1
kind: Service
metadata:
  name: demo-nodeport
  namespace: svc-types
spec:
  type: NodePort
  selector:
    app: svc-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
