apiVersion: v1
kind: Service
metadata:
  name: demo-clusterip
  namespace: svc-types
spec:
  type: ClusterIP
  selector:
    app: svc-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
