apiVersion: v1
kind: Service
metadata:
  name: demo-loadbalancer
  namespace: svc-types
spec:
  type: LoadBalancer
  selector:
    app: svc-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
