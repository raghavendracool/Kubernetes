# 01 — Prerequisites

```bash
aws --version
kubectl version --client
eksctl version
helm version
aws sts get-caller-identity
```

Course examples:

```text
Region: ap-south-1
Cluster: sdhub-k8s
Kubernetes: 1.36
```

Use a training AWS account.
