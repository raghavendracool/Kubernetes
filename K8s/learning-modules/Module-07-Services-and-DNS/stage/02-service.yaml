apiVersion: v1
kind: Service
metadata:
  name: student-web
  namespace: student-app
spec:
  type: ClusterIP
  selector:
    app: student-web
  ports:
    - name: http
      port: 80
      targetPort: 80
